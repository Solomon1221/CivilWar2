using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class City : MonoBehaviour
{
    // Start is called before the first frame update
    // money is kept in gold
    float annualRevenue;

    public bool hasAirport;
    public bool hasPort;

    string CityName;



    // infrastructure is like health for a city, can be destroyed
    float infrastructure = 100;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
